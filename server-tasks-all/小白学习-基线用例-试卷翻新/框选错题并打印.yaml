android:

tasks:
  - name: 框选错题并打印
    # baseline.goal: 验证框选错题并打印
    # baseline.start_page: App 首页
    # baseline.path: 试卷翻新 icon -> 相册导入 -> 完成 -> 框选错题 -> 手动框题 -> 确定 -> 修改 -> 确定
    # baseline.expected: 框选错题并打印相关页面结果符合预期
    # baseline.repair_hint: 优先参考当前 App 的页面知识库和基线辅助截图；如果入口文案变化，使用真实可见文案修复步骤和断言。
    flow:
      - runAdbShell: "input keyevent 3"
      - sleep: 500
      - runAdbShell: "input keyevent 187"
      - sleep: 1000
      - runAdbShell: "input swipe 540 1900 540 350 300"
      - sleep: 500
      - runAdbShell: "input swipe 540 1900 540 350 300"
      - sleep: 500
      - runAdbShell: "input swipe 540 1900 540 350 300"
      - sleep: 500
      - runAdbShell: "input keyevent 3"
      - sleep: 500
      - runAdbShell: "am kill-all"
      - sleep: 500
      - runAdbShell: "am force-stop com.xbxxhz.box"
      - sleep: 1500
      - launch: com.xbxxhz.box
      - sleep: 3000
      - sleep: 3000
      - ai: "如果出现 您还未登录，请先登录提示，等待一下，没有就跳过"
      - sleep: 1000
      - ai: "打开之后如果有弹窗就找到x按钮点击关闭，没有弹窗就跳过"
      - sleep: 3000
      - aiTap: "试卷翻新 icon"
      - sleep: 3000
      - ai: "打开之后如果弹出相机权限弹窗点击\"确定\"按钮后再点击“使用应用时允许”按钮，没有弹窗就跳过"
      - sleep: 3000
      - ai: "如果仍有弹窗点击“拍摄试卷”按钮，没有弹窗就跳过"
      - sleep: 3000
      - aiTap: "相册导入"
      - sleep: 1000
      - ai: "打开之后如果弹出存储权限弹窗点击\"确定\"按钮后再点击“允许”按钮，没有弹窗就跳过"
      - sleep: 2000
      - ai: "勾选一张照片即可，不要点击完成按钮"
      - sleep: 1000
      - aiTap: "完成"
      - aiWaitFor: "页面加载完成且没有明显加载中状态"
        timeout: 30000
      - ai: "打开之后如果弹出新手引导弹窗点击\"我知道了\"按钮，没有弹窗就跳过"
      - sleep: 1000
      - aiTap: "框选错题"
      - aiWaitFor: "页面加载完成且没有明显加载中状态"
        timeout: 60000
      - ai: "打开之后如果弹出加入错题夹弹窗点击\"确定\"按钮，没有弹窗就跳过"
      - aiWaitFor: "页面加载完成，并且可以继续执行下一步：手动框题"
        timeout: 60000
      - aiTap: "手动框题"
      - sleep: 3000
      - aiTap: "确定"
      - sleep: 3000
      - aiTap: "修改"
      - sleep: 3000
      - aiTap: "确定"
      - sleep: 3000
      - ai: "点击保存错题按钮"
      - aiWaitFor: "页面加载完成，并且可以继续执行下一步：打印错题"
        timeout: 60000
      - aiTap: "打印错题"
      - aiWaitFor: "页面加载完成且没有明显加载中状态"
        timeout: 60000
      - ai: "打开之后如果弹出新手引导弹窗则逐步点击关闭按钮，没有弹窗就跳过"
      - sleep: 1000
      - aiTap: "立即打印"
      - aiWaitFor: "页面加载完成，并且可以继续执行下一步：确认打印"
        timeout: 60000
      - aiTap: "确认打印"
      - sleep: 1000
      - runAdbShell: "am force-stop com.xbxxhz.box"
      - sleep: 1000
