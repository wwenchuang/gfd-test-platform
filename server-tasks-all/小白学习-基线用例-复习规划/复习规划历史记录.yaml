android:

tasks:
  - name: 复习规划历史记录
    # baseline.case_id: COM_XBXXHZ_BOX_a2fb5b3e68d5
    # baseline.goal: 验证复习规划历史记录
    # baseline.start_page: App 首页
    # baseline.path: 确定 -> 当前页面的搜索输入框或文本输入框 -> 搜索 -> 推荐 tab -> “精准复习规划”按钮 -> 规划记录 -> 任一视频封面 -> 立即打印
    # baseline.expected: 复习规划历史记录相关页面结果符合预期
    # baseline.repair_hint: 优先参考当前 App 的页面知识库和基线辅助截图；如果入口文案变化，使用真实可见文案修复步骤和断言。
    flow:
      - runAdbShell: "input keyevent 3"
      - sleep: 500
      - runAdbShell: "input keyevent 187"
      - sleep: 1000
      - runAdbShell: "input swipe 540 1900 540 350 300"
      - sleep: 500
      - runAdbShell: "input swipe 540 1900 540 350 300"
      - sleep: 500
      - runAdbShell: "input swipe 540 1900 540 350 300"
      - sleep: 500
      - runAdbShell: "input keyevent 3"
      - sleep: 500
      - runAdbShell: "am kill-all"
      - sleep: 500
      - runAdbShell: "am force-stop com.xbxxhz.box"
      - sleep: 1500
      - launch: com.xbxxhz.box
      - sleep: 3000
      - sleep: 3000
      - ai: "如果出现 您还未登录，请先登录提示，等待一下，没有就跳过"
      - sleep: 1000
      - ai: "打开之后如果有弹窗就找到x按钮点击关闭，没有弹窗就跳过"
      - sleep: 3000
      - ai: "点击顶部年级切换按钮"
      - sleep: 3000
      - ai: "点击一年级选项"
      - aiTap: "确定"
      - sleep: 3000
      - ai: "点击搜索框进入搜索页面"
      - sleep: 2000
      - aiTap: "当前页面的搜索输入框或文本输入框"
      - sleep: 300
      - aiInput: "当前页面的搜索输入框或文本输入框"
        value: "精准复习规划"
      - sleep: 500
      - aiTap: "搜索"
      - sleep: 3000
      - aiTap: "推荐 tab"
      - sleep: 2000
      - aiTap: "“精准复习规划”按钮"
      - sleep: 3000
      - aiTap: "规划记录"
      - sleep: 3000
      - ai: "点击规划记录列表第一条记录"
      - sleep: 3000
      - ai: "打开之后如果弹出弹窗点击关闭按钮，没有弹窗就跳过"
      - sleep: 2000
      - aiScroll: "当前页面主要内容区域，不要滚动顶部栏或底部导航"
        scrollType: "singleAction"
        direction: "up"
        distance: 500
        deepLocate: true
      - aiWaitFor: "页面中出现知识点精讲视频"
        timeout: 30000
      - aiTap: "任一视频封面"
      - ai: "点击一次返回键"
      - sleep: 2000
      - aiScroll: "当前页面主要内容区域，不要滚动顶部栏或底部导航"
        scrollType: "singleAction"
        direction: "down"
        distance: 500
        deepLocate: true
      - aiWaitFor: "页面中出现「智能组卷」按钮"
        timeout: 30000
      - aiTap: "智能组卷按钮"
      - aiWaitFor: "页面加载完成，并且可以继续执行下一步：立即打印"
        timeout: 60000
      - aiTap: "立即打印"
      - sleep: 2000
      - aiTap: "确认打印"
      - sleep: 1000
      - runAdbShell: "am force-stop com.xbxxhz.box"
      - sleep: 1000
