android:

tasks:
  - name: 生成复习规划
    # baseline.case_id: COM_XBXXHZ_BOX_96fb514bbea1
    # baseline.goal: 验证生成复习规划
    # baseline.start_page: App 首页
    # baseline.path: 当前页面的搜索输入框或文本输入框 -> 推荐 tab -> 选择错题 -> 确认选择 -> 全选 -> 下一步 -> 切换 -> 生成精准复习规划
    # baseline.expected: 生成复习规划相关页面结果符合预期
    # baseline.repair_hint: 优先参考当前 App 的页面知识库和基线辅助截图；如果入口文案变化，使用真实可见文案修复步骤和断言。
    flow:
      - runAdbShell: "input keyevent 3"
      - sleep: 500
      - runAdbShell: "input keyevent 187"
      - sleep: 1000
      - runAdbShell: "input swipe 540 1900 540 350 300"
      - sleep: 500
      - runAdbShell: "input swipe 540 1900 540 350 300"
      - sleep: 500
      - runAdbShell: "input swipe 540 1900 540 350 300"
      - sleep: 500
      - runAdbShell: "input keyevent 3"
      - sleep: 500
      - runAdbShell: "am kill-all"
      - sleep: 500
      - runAdbShell: "am force-stop com.xbxxhz.box"
      - sleep: 1500
      - launch: com.xbxxhz.box
      - sleep: 3000
      - sleep: 3000
      - ai: "如果出现 您还未登录，请先登录提示，等待一下，没有就跳过"
      - sleep: 1000
      - ai: "打开之后如果有弹窗就找到x按钮点击关闭，没有弹窗就跳过"
      - sleep: 3000
      - ai: "点击顶部年级切换按钮"
      - sleep: 3000
      - ai: "点击一年级选项后点击一次确定按钮"
      - sleep: 3000
      - ai: "点击搜索框进入搜索页面"
      - aiInput: "当前页面的搜索输入框或文本输入框"
        value: “精准复习规划”
      - sleep: 300
      - aiKeyboardPress: "当前页面的搜索输入框或文本输入框"
        keyName: "Enter"
      - sleep: 500
      - sleep: 3000
      - aiTap: "推荐 tab"
      - sleep: 2000
      - ai: "点击“精准复习规划”按钮"
      - sleep: 2000
      - aiTap: "选择错题"
      - sleep: 2000
      - ai: "点击科目下拉框"
      - sleep: 2000
      - ai: "点击一次确定按钮，不需要点击确认选择按钮"
      - sleep: 2000
      - aiTap: "确认选择"
      - aiWaitFor: "页面加载完成，并且可以继续执行下一步：全选"
        timeout: 60000
      - aiTap: "全选"
      - sleep: 2000
      - aiTap: "下一步"
      - sleep: 2000
      - aiTap: "切换"
      - sleep: 2000
      - ai: "教材版本选择“统编版”后点击确认按钮"
      - sleep: 2000
      - aiTap: "生成精准复习规划"
      - aiWaitFor: "页面加载完成且没有明显加载中状态"
        timeout: 30000
      - ai: "打开之后如果有弹窗就找到x按钮点击关闭，没有弹窗就跳过"
      - aiWaitFor: "页面加载完成且没有明显加载中状态"
        timeout: 30000
      - ai: "打开之后如果有弹窗就找到x按钮点击关闭，没有弹窗就跳过"
      - sleep: 1000
      - aiScroll: "当前页面主要内容区域，不要滚动顶部栏或底部导航"
        scrollType: "singleAction"
        direction: "down"
        distance: 500
        deepLocate: true
      - aiWaitFor: "页面中出现知识点精讲视频封面"
        timeout: 30000
      - aiTap: "任一知识点精讲视频封面"
      - aiWaitFor: "页面加载完成且没有明显加载中状态"
        timeout: 60000
      - ai: "点击一次返回键"
      - sleep: 1000
      - aiScroll: "当前页面主要内容区域，不要滚动顶部栏或底部导航"
        scrollType: "singleAction"
        direction: "down"
        distance: 500
        deepLocate: true
      - aiWaitFor: "页面中出现「举一反三」按钮"
        timeout: 30000
      - aiTap: "举一反三按钮"
      - aiWaitFor: "页面加载完成，并且可以继续执行下一步：立即打印"
        timeout: 60000
      - aiTap: "立即打印"
      - sleep: 2000
      - aiTap: "确认打印"
      - sleep: 1000
      - runAdbShell: "am force-stop com.xbxxhz.box"
      - sleep: 1000
