# API 测试平台体验点检与改进计划

## 点检范围

2026-08-26 使用全新浏览器页面，从任务平台登录后进入 API 测试平台，按真实数据依次检查：工作台、接口资产、用例管理、任务管理、基线用例、定时任务、执行记录、测试报告和环境配置。首次实站点检只做读取和页面切换；修改后在本地隔离数据中点击了任务恢复、用例调试、基线回归、报告查看、定时任务执行详情和飞书测试发送等核心操作，不删除线上已有数据，也不向线上机器人发送测试消息。

## 已确认的问题

### P0：结果可信度

- 测试报告会把 `239 / 240`、`716 / 719` 显示为 `100%`。这会误导报告阅读者，任何存在失败、异常、跳过或取消的执行都不能展示为满分。
- 定时任务列表只显示原始 Cron，没有下次执行时间、最近触发时间和最近结果。用户无法在列表中判断调度是否生效。

### P1：状态可追踪

- 任务列表存在重名任务，但只有接口数、基线数、环境和状态，缺少更新时间、最近执行结果和执行入口，无法快速区分同名任务。
- 任务详情显示原始英文状态 `ready`，与列表的中文状态不一致。
- 基线列表展示内部采纳原因 `passing debug evidence`，不应把实现文案暴露给用户。
- 飞书机器人配置只有保存，没有“发送测试通知”。用户必须等到一次真实执行后才能知道机器人是否可用。

### P1：操作语义

- 工作台同时出现多个“保存”动作，任务、范围、草稿和执行的对象边界不够清晰。
- 用例详情存在“保存并调试”和“保存并执行”两套相近动作，需要明确唯一主路径，避免新手误以为会产生两种不同执行结果。
- 接口资产页实际承担同步源和版本管理，入口文案需要强调资产来源与版本，而不是让用户误以为这里直接管理所有接口用例。

### P2：长列表效率

- 基线页真实数据超过 280 条、执行记录超过 50 条，一次展示全部内容影响定位和渲染。
- 用例页已有分组、筛选、搜索和独立详情，信息架构正确；不应再把完整用例列表塞回工作台。

### P2：配置可理解性

- 环境详情中多个服务键可能映射到同一个地址，也可能存在未配置地址。现有汇总已基本可用，后续可增加主动连通性检测，但必须明确检测方法和鉴权副作用后再实现。
- 登录成功后旧的失败提示可能残留，属于上层任务平台登录体验问题，不在本轮 API 测试前端范围内。

## 信息架构结论

- 左侧现有九个入口已经覆盖“设计、资产、用例、任务、基线、调度、执行、报告、配置”完整生命周期。
- 本轮不新增重复的“总览”或“管理中心”入口。报告页继续承担项目质量总览，工作台继续承担单次设计和调试；用例管理与任务管理保持独立页面。
- 优先补齐每个资产的状态、最近运行证据和直接下一步操作，比增加导航项更能缩短使用路径。

## 实施顺序

1. 修复通过率展示，保证异常执行不会显示 `100%`。
2. 为定时任务补充人类可读计划、时区、下次执行、最近触发和最近结果，并可跳转对应执行记录。
3. 为任务列表补充更新时间和最近执行摘要，统一状态中文展示。
4. 本地化基线采纳原因，清理内部技术文案。
5. 为项目飞书配置增加显式测试发送能力，不关联任何真实测试执行。
6. 收敛重复保存/执行动作，并补齐按钮说明。
7. 完成单元测试、静态检查、生产构建和真实浏览器回归。

## 暂不实施

- 不新增重复左侧入口。
- 不在没有探测契约前直接增加环境“连通性检查”，避免探测请求产生业务副作用。
- 不改变工作台、用例、任务和报告现有路由，不迁移用户数据。

## 实施结果

- 已修复通过率取整问题：只要通过数小于总数，页面和飞书卡片都不会显示 `100%`。
- 已为定时任务补充可读周期、服务端时区、下次执行时间、最近触发方式、最近执行结果和对应执行记录入口。
- 已为任务列表和详情补充最近执行结果、通过率、更新时间，并统一任务状态的中文展示；最近执行摘要独立读取，不会被后续 AI 生成摘要覆盖。
- 已将基线内部采纳原因 `passing debug evidence` 本地化为面向用户的文案。
- 已为项目飞书配置增加“发送测试通知”，测试卡片明确标记为配置验证且不关联测试执行。
- 飞书配置和发送只接受飞书/Lark 官方 HTTPS 机器人地址，拒绝 HTTP、私网、回环、伪造域名和非机器人路径。
- 已修复用例编辑器“保存并调试”事件未接通的问题，并移除页面外层重复的“保存并执行”入口。
- 已为基线和执行记录增加分页，避免一次渲染完整长列表。
- 已补齐手机和平板导航、上下文区折叠、工作台面板切换，以及用例、任务、报告的移动端列表/详情切换。
- 左侧九个入口保持不变；实站点检确认生命周期覆盖完整，没有新增一个与现有页面职责重叠的入口。

## 验证结果

```bash
bash tests/run_api_testing_gate.sh
# 449 backend tests passed
# 54 frontend files / 279 tests passed
# production build and visual checks passed
# 1 Chromium end-to-end workflow passed

npm --prefix api-testing-ui test -- --run
# 54 files / 279 tests passed

npm --prefix api-testing-ui run build
# vue-tsc + Vite production build passed

node tests/api_testing_ui_visual_check.js
# desktop / compact desktop / tablet / mobile / settings mobile passed

npx playwright test tests/api_testing_e2e.spec.mjs --project=chromium
# 1 Chromium end-to-end workflow passed

.venv/bin/python tests/backend_static_checks.py
# 63 checks passed

.venv/bin/python tests/frontend_static_checks.py
# 84 checks passed
```

## 第二轮全流程实站点检

2026-08-26 再次使用独立 Chrome 页面登录 `http://101.34.197.12:8088/task-manager.html`，对 API 测试平台九个导航入口和页面间跳转进行真实点击。点检不是只读浏览：创建了临时任务、选取真实接口、生成基础与 AI 用例、保存并调试、采纳基线、执行任务、创建定时任务并手动触发，随后从执行记录和报告页回看本次证据。

### 已完成的点击链路

| 阶段 | 实际操作 | 结果 |
| --- | --- | --- |
| 接口资产 | 切换项目、接口版本和环境，展开同步与 OpenAPI 导入区，进入项目编辑 | 入口可达；发现项目编辑缺少取消操作 |
| 工作台 | 创建临时任务，搜索并选择真实接口，查看接口详情，添加前置步骤并搜索接口 | 选择、搜索高亮和步骤编排可用；发现深链恢复期间短暂显示旧状态 |
| 基础正向生成 | 生成、编辑、保存并调试，检查请求、响应和业务断言 | 调试通过，HTTP 200、`$.code == 0`、`$.data exists` 均有证据 |
| AI 生成 | 选择“补充业务断言”意图并等待任务完成 | 生成 5 个草稿；发现等待过程缺少耗时反馈，工作台只突出一个结果 |
| 基线 | 将真实调试通过用例采纳为基线，检查分组、搜索、分页与操作区 | 可用；发现同一接口多版本基线的计数和任务展示不一致 |
| 任务管理 | 搜索、选择、编辑任务，查看接口范围并执行 | 执行成功；实际运行 2 个基线版本，但页面只显示 1 个接口，语义容易误判 |
| 定时任务 | 创建禁用的 Cron 临时任务，选择多基线目标，验证表达式说明并手动执行 | 手动执行通过 1/1；发现目标列表重复项难区分、任务卡暴露 UUID |
| 执行记录 | 通过任务、定时任务和接口详情跨页进入指定执行，切换实时轨迹、用例结果、技术日志 | 可达；发现新执行加载前会短暂显示旧执行详情 |
| 测试报告 | 选择本次报告，进入项目驾驶舱和完整诊断，切换失败分类与证据 | 可达；发现默认报告进入完整诊断时 URL 未必携带当前执行 ID |
| 环境配置 | 切换项目、环境、服务、变量和版本历史，检查飞书配置入口 | 页面可用；本轮未向线上群发送测试消息 |

### 第二轮已实现

- 任务同时显示“覆盖接口数”和“实际执行用例版本数”，执行范围不再把同一接口的多个有效基线折叠成一条。
- 执行深链进入时立即清空旧详情并显示“正在读取本次执行”，避免误读上一条执行结果。
- 接口详情把请求参数和响应契约结构化展示，保留可折叠原始 JSON，并可直接进入该接口的执行记录。
- 执行记录支持按接口筛选，跨页后显示当前筛选条件并可一键清除。
- AI 意图改为明确的“意图模板”，生成期间显示耗时；完成后列出本批全部草稿，可逐条打开或统一进入用例管理。
- 定时任务的多基线选项展示用例版本、来源、接口方法、路径和采纳时间；分组默认折叠，搜索时自动展开。
- 定时任务卡片使用可读目标摘要，不再直接展示内部 UUID。
- 报告“查看完整诊断”先选中当前报告，再写入执行 ID 深链。
- 项目编辑增加取消操作；工作台恢复任务、接口版本和环境完成前显示统一加载态。
- 新增结构化接口详情的窄屏布局，字段和值可换行，不产生横向溢出。

### 验收边界

- 实站已验证线上数据和真实后端的完整操作链；修改后的代码通过本地隔离后端继续验证全导航、移动端、导入、生成、调试、基线、执行和报告闭环。
- 飞书“发送测试通知”会向真实外部群发送消息，本轮没有触发；只检查了配置、启停和入口状态。
- 第二轮创建的临时对象名称均带 `Codex` 和日期标记，便于验收结束后集中删除，不与已有业务数据混淆。

## 第三轮：前置步骤配置闭环

### 问题记录

- 添加前置步骤后，用户只能手写 JSON Path，无法先调用接口查看真实返回，也无法判断变量是否能被后续步骤使用。
- Token 等动态字段虽然可以通过提取规则传递，但编辑器不展示真实响应值，新手无法完成临时替换和后续链路验证。
- 接口返回字段多时缺少搜索、类型和路径信息，直接展开原始 JSON 不适合逐项选择。
- 写接口试运行存在真实副作用，原页面没有在操作前说明清理责任。

### 已实现

- 每个启用的前置步骤提供“试运行到此步骤”，顺序执行到目标步骤并展示实际到达位置、缺失变量、失败原因和可选响应字段。
- 返回字段支持按路径、名称和值搜索，展示来源、类型和建议变量名；勾选后直接生成已有输出变量规则。
- 敏感字段默认密码框展示，只有主动点击后才显示原值；允许编辑替换并继续预览后续步骤。
- 临时替换值只保存在当前页面会话并按仍然存在的输出变量过滤，不写入用例；删除或重命名提取规则后不会把失效替换值发送到后端。
- 写操作显示副作用提示，完整执行仍要求使用清理步骤回收本次创建或修改的数据。
- 预览 API 禁止缓存，服务端只返回请求作用域内的原始响应；正式执行轨迹和持久化结果继续脱敏。
- 响应面板在桌面、平板和手机均可操作；移动端完整诊断和长执行 URL 不再产生横向溢出。

### 验收证据

- 后端覆盖：预览载荷校验、启用步骤前缀、真实变量传递、临时替换、失败阻断、响应字段上限和敏感字段识别。
- 前端覆盖：字段搜索、变量名校验、敏感值显示/隐藏、临时替换不进入用例、响应字段转提取规则。
- 浏览器视觉覆盖：字段面板打开状态的 1440px 和 390px 截图，均检查无横向溢出。
- Chromium 真实闭环：隔离 PostgreSQL、Redis、Celery 和本地 HTTP 目标中运行收藏列表前置步骤，选择真实 `$.data[0].id` 后继续保存调试及后续完整流程。
