# 录制历史分组、批量删除和响应式布局

目标：历史按应用、模块分组，可筛选并批量删除；小窗口顶部按钮完整可达。

用户已要求直接实施并验证，不重复设计审批。沿用现有录制持久化和单条删除鉴权，不改变手机录制协议。

- [x] 后端：录制创建保存可选 module_name；新增本人记录模块归属更新接口，验证模块属于应用。历史接口按 100 条分页返回轻量摘要（无截图、UI XML 和完整 YAML），前端读取所有分页，不再只看最近 30 条。
- [x] 前端：历史按应用→模块展示、应用及模块级联筛选，未归属历史显示未分组。支持勾选、全选当前筛选结果、删除计数及确认；顺序复用鉴权删除接口，进行中的记录不可选，失败项保留可重试。新建与历史详情都可选所属模块，保存 YAML 同步模块。
- [x] 布局：录制页子项采用内容高度，页面整体滚动；窄屏单列、工具栏换行、卡片最小宽度不超过容器。
- [x] 验证：先补 Node/Python 失败测试，再实施；检查分页不漏记录、越权与串应用拒绝、批量部分失败与取消、筛选全选范围、持久化；真实浏览器验证桌面和小屏按钮边界。
- [x] 运行仓库必跑检查，更新 CODEX_STATE；部署 QA 并用 Chrome 检查历史分组、工具栏和可丢弃测试记录的批量删除。

验收边界：删除录制及截图不删除已保存的用例 YAML；历史无模块数据不猜测归属，由用户补选。Windows CLI 依赖与回放属于单独验收，不能用历史页面验收替代；用户修复后的实机稳定性结果另记 docs/evidence/recorder-stability-2026-09-24.md。

2026-09-24 QA 验收：线上 45019ff，Chrome 实测应用/模块过滤、筛选切换取消旧勾选、跨模块选择两条测试记录批量删除通过；服务端复查已删除且原 41 条记录保留。
