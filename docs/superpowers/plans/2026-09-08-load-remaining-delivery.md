# 性能测试剩余能力交付计划

**Goal:** 完成已部署版本真实验收，并把定时性能执行、Pod状态和异步清理的缺口逐项落到可检查的软件能力。
**Architecture:** 复用现有压测草稿/连通性/预检/启动、监控配置及报告机制。固定版本快照、权限重新核验、分布式锁和有界执行贯穿自动流程；不另建能绕过门禁的发压入口。

- [x] A 线上验收：确认c788b49；Safari用已验证只读场景，固定吞吐总2/s、20秒、2VU、两节点明确共同参与；核对实际分片、资源样本、结束报告及AI状态。验证建议跳转不自动执行。保留执行ID。
- [x] B 定时性能执行与通知：单独性能计划（不混入功能基线），从已审查配置建立默认停用计划；冻结场景/环境/压力/节点/监控/停止条件；执行时重验权限、节点及预检；禁止重叠运行、补发过期高压、无界重试；通知显式选择，发送失败与压测结论分离。需要配置页面、权限、测试。
- [x] C Pod状态：在已有Prometheus配置中增加kube-state-metrics就绪/重启指标，精确namespace和Pod，按实例展示；缺失不作0，重启为窗口增量且处理counter重置，主机/容器/Pod口径分开。配置、采集、报告、说明和测试一起检查。
- [ ] D 异步终态与补偿：先核对现有compiler支持的poll/cleanup边界，采用有界轮询、明确终态断言与本次资源ID清理；不可对历史资源或未知归属执行补偿。具体实现前记录契约和测试。不得因节点断线盲目重发创建请求。
- [ ] E 真实监控接入：只读查询现有环境配置；无数据源时明确所缺URL、标签、只读授权，提供部署及网页配置说明。不拿压力机资源或模拟Prometheus当业务验收。
- [ ] F 集成验收：相关数据库测试、完整前端测试、静态/构建、浏览器交互与独立审查；main提交推送，CODEX_STATE记录已完成与仍需环境配合项。

本轮线上不启用定时发压、不发送飞书消息、不对写入业务做高压；仅执行先前获准的只读短跑。用户原有Word与docs/career保留。

验收结果见 docs/性能测试剩余能力验收-2026-09-08.md。D 本轮交付当轮资源安全清理；通用异步轮询和强杀持久补偿仍未完成。E 缺真实监控源，已询问配置。A 的阶梯建议后续配置属于本轮本地新增，不冒充线上已部署。
