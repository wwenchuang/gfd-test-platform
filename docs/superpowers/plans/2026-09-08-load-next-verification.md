# 下一轮验证与多节点低流量验收

## 本轮范围与验收

1. 固定吞吐增加所有选中节点参与模式：拆分全局 rate 与最大 VU，每台至少分配一个；不足、离线、过期校准、备用未授权均禁止创建。默认仍按节点优先级分配。检查真实数据库分片和全局总数。
2. AI 证据引用、结论数值或确定性结论冲突时，仅进行一次带校验原因的纠正请求。校验始终对原证据执行；失败仍安全回退，基础报告不受影响。升级提示词版本以避免复用旧诊断。
3. 报告“按建议配置下一轮”只导航到配置，不创建任务或自动发压。重新读取授权范围内的原执行与当前诊断；核对关联、应用、场景和环境，保留原版本、阈值、监控必选性和观察窗口、测试条件及停止策略。节点重新选择、生产确认重新勾选，沿用连通性与预检门禁。
4. 固定并发/固定吞吐可预填；阶梯建议没有阶段明细时明确提示补齐，不根据总目标伪造阶段。吞吐按每秒展示，旧轮每分钟单位独立标注。
5. 回归测试、构建、静态检查、Safari报告入口→预填→确认门禁→返回；提交前审查，main提交推送。浏览器本地夹具与线上真实执行分开记录。

## 线上升级核对（2026-09-08 14:45）

Safari节点页与API已确认两台Agent都是0.1.5、心跳在线、校准有效；各800VU/2400次每秒可分配。本机本次校准有效至9月15日14:41。平台当前88517c5。

## 保留的未完成边界

- 新功能部署后，真实同时两节点分片验收、升级后本机运行CPU/内存报告、真实模型纠正成功率仍需验证；本地测试不能替代线上。
- 真实业务监控源尚未接入；压力机资源不能冒充被测服务资源。
- 定时性能发压与自动通知、Pod就绪/重启统计、跨Pod聚合、通用异步补偿不属于本次三个修复，仍未完成。
- 本次建议跟跑保留原环境/阈值/监控，不支持跨应用自动切换。切换到原应用后重进，或使用普通新建入口自定义条件。
- 数据集仍需通过平台实时状态和容量校验；历史版本引用不保证外部数据和业务状态不变。

## 本地交付检查

- 前端602项/85文件、后端隔离数据库及分配与AI51项通过；静态63/84、Python编译、生产构建通过。Vite仍提示主包大于500kB。
- 独立审查两项P2：奇数目标按比例取整误拒绝、低吞吐每分钟建议被拒绝，均修复。
- Safari原生点击报告建议、读取2次秒/60秒预填、节点与生产确认门禁、刷新恢复及返回清理查询参数。测试API为本地夹具，没有对业务发压。响应式专项通过。
- 部署：在服务器main源码目录执行git pull --ff-only origin main，再运行bash deploy/update-main-server.sh；本次无Agent代码更新，两台0.1.5不必再次升级。
