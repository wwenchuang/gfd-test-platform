# 录制页设备实时状态与紧凑布局

用户授权直接实施。采用已有 /runners 状态接口的 5 秒轻量轮询，不另建 WebSocket、不刷新整页。

- [x] 先补 UI 回归：空闲/占用/离线/未知，掉线保留，刷新失败标过期，恢复成功；无并发请求；隐藏/离页停查，返回即时查；输入和截图 DOM 不变。
- [x] js/device-recorder.js：独立完整设备快照，不改变共用 runnerDevices 的在线过滤规则；8 秒请求超时；仅更新设备容器及开始按钮；显示最近更新时间及手动刷新。当前录制手机标识保留，不自动选其他手机。
- [x] css/app.css：设备紧凑行，绿色空闲/琥珀占用/红色离线/灰色未知；状态文字辅助颜色；设置栏和输入样式统一，长序列号换行，窄屏不溢出。更新资源版本。
- [x] 用户追加的卡片与截图问题：取消时间线隐式网格拉伸，标题和导航按内容高度；移除详情 430px 内部滚动，整页可滚动到截图底部和编辑区。
- [x] Chrome 发现同一手机旧/新 Runner 重复：按序列号去重，优先在线 Runner，并补双方输入顺序的回归。
- [x] 40 项 UI、三种视口、前端 84 项和后端 63 项检查通过。Chrome 堡垒机部署 27832fc，健康 revision/资源版本核对，Chrome 历史截图、编辑保持展开和设备自动刷新通过；未产生手机触摸或业务删除。
- [x] CODEX_STATE 记录证据和限制：每 5 秒取服务端最新已知状态，受 Runner/Sonic 上报延迟影响，不承诺瞬时感知物理掉线。
