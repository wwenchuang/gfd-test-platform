# 每日基线条件恢复计划

**目标：** 修正成长报告与组件详情的数据依赖，并使固定测试设备的切片取消、创建与清理形成可复验闭环。

**设计：** 给功能用例的前置及清理步骤增加可选 `only_if_variable`。变量不存在或为 null、空字符串、空数组、空对象时记录中文原因并跳过；0 和 false 是有效值。未配置条件的历史步骤行为不变。条件仅决定是否发请求，不替代业务断言、设备身份或最终待机校验。前置试运行与完整执行采用同一判定。变量名接受严格格式及静态来源校验。复制到尚不支持条件的压测场景时拒绝，不能静默删除条件。

**方案取舍：** 单纯延长轮询不能清理切片；无条件取消在无任务时不成立；本次采用最小的有值条件，不引入表达式或脚本执行。

**约束：** 仅使用 9888E0094F2A。必须先确认当前任务属于指定设备，再取消并验证恢复；不得从“slicing”直接放宽到允许打印。保留业务码、身份和结构断言。保留 Word 与晋升材料。在 main 修改。

## 实施与验证

- [x] 合同及执行器：先在 `test_case_contract.py`、`test_executor.py` 增加有值/空值/非法条件、前置/清理/预览测试；确认失败后修改 `contracts/case.py`、`executor.py`。
- [x] 验证和压测边界：`validation.py` 校验条件变量来源；`load_scenario_service.py` 拒绝转换任何条件步骤（含停用）；打印清理策略仅接受由本轮任务变量约束的取消。补相应回归测试。
- [x] 前端：`contracts.ts`、`InlineWorkflowStepEditor.vue`、`WorkflowStepCard.vue` 增加中文条件编辑和摘要；验证保存及清除条件、构建和静态检查。
- [x] Safari：登录恢复后读取最新执行及接口契约，修正并定向复验四条基线。用原每日精确范围完整执行，记录 ID、总数与设备恢复证据。
- [x] 更新 CODEX_STATE.md；代码已提交推送并发布，线上验收与未通过项分别记录，最终交接单独提交。

## 现场状态

初始状态：2026-09-21 本地与远端 main 为 010c873，线上 health 为 25ea12a。Safari 登录入口空白，先恢复登录再执行。

现场复验：代码 ad4c81f 已推送并部署，线上 health 与 Safari 中文条件配置均确认。原四条基线各两次定向通过，两条打印分别验证无切片条件跳过；另用批准种子完成有切片分支的创建、同任务身份验证、取消、清空、待机和清理预览。

精确每日297条执行69fa0e15-0901-4f4e-af1f-25f1ef2437dd为296/297，原四项全部通过。共享5ba0cfd2-1b8d-445f-a116-dd37af2a587c为10/10，合计306/307。

新增暴露的CAD失败保持原证据：150秒生成未完成、删除业务失败；后来按ID读到成功，定向回收后同ID返回“生成记录不存在”。候选0cce5676定向等待约5分钟仍未完成，因此不能仅靠延长等待解决；数字ID拼接草稿错误已修正，候选v11未采纳。旧对象数组不包含数字ID的清理断言无效，候选改为详情/列表精确ID和删除后同ID不存在检查，但原v7仍为有效版本，不能声称已替换。两个本轮任务均经完成态身份核对后回收；无依据重复创建已停止。CAD业务服务慢任务排查及候选完整验收仍待完成，详见CODEX_STATE.md首节。
