# 手机设备卡片实施计划

目标：将 Agent 工作台设备下拉框改为手机卡片，保持当前单设备执行合同与表单状态。用户已要求直接完成最佳实现，不再增加设计审批。

设计：具体手机采用原生单选控件、勾选视觉与整卡点击，展示手机轮廓、名称、在线状态、系统、分辨率、当前应用版本。自动分配单独展示；设备编号和执行器版本折叠。空列表显示恢复指引；已选设备消失时保留选择并阻断执行，不自动换机。现有隐藏 select 保留为执行和表单持久化的数据源，不引入多任务分发。用户已异步询问是否需要多选；未收到前按现有单设备语义实现。

- [x] 在现有脚本内补卡片渲染与选择同步，状态刷新与应用切换不丢失选择。
- [x] 增加有意义的 DOM 行为测试：点击更新执行参数、应用版本、离线选择阻断与安全文本显示。
- [x] 前端静态检查、脚本语法与表单回归，Safari 本地真实组件验收。
- [x] 更新交接，main 提交推送，明确发布状态。

验收不实际发送手机任务或操作3D打印设备。保留用户 Word 及晋升资料。

结果：10项DOM/表单回归与前后端静态检查通过，Safari本地验收已完成。发布状态以CODEX_STATE首节为准。
