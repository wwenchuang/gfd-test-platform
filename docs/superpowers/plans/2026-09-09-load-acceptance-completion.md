# 压测验收与阶段建议补齐

本轮按用户已明确的方案在 main 完成；保留三份未跟踪 Word 和 docs/career，不重跑已验收的家用基线。

## 核对结果

- 本地 main、远端 main、线上 /api/health 均为 dfa1126a36235c6be58f635a9e3760409be174a6。
- Worker 恢复与最终 288/288 已有验收，旧顶部待部署记录由末尾线上记录覆盖。
- 四种模型和公开只读双节点已有历史运行；本轮需补演示服务目标与监控归属、失败/停止、阶段建议真实闭环。

## 实现设计

执行历时只由 started_at / finished_at 计算；用例累计耗时独立求和，缺失不互相替代。

下一轮沿用原模型与完整 workload（开始压力、单位、各阶段、VU预算），保留原曲线及逐阶段前后差异。AI 提出参数并引用现有证据；平台校验结构、场景副作用、当前失败、采样、资源、用户目标与增长上限。校验通过采用 AI，越界则记录原建议及原因并调整或回退。没有升压依据仍给出原曲线复验、排查失败、稳态或恢复观察计划。节点实时容量继续由创建、连通性、预检门禁检查，不自动发压。

## 执行清单

- [x] 耗时口径：并行耗时与缺时间戳测试，修复工具函数和执行/报告展示。
- [x] 阶梯策略：未达压、失败、资源异常、曲线保留与 AI 建议校验测试；扩展策略和 AI 契约，升级提示词。
- [x] 阶段向导：开始压力/分钟单位/降压零目标和原监控阈值保留，贯通报告差异、完整阶段预填。
- [x] 本地检查：压测 PostgreSQL 专项 147 项、Vue 88 文件 636 项、前后端静态 63/84 项、AI Gateway 静态 46 项、生产构建、语法/diff 检查；独立审查问题已修复。
- [ ] 线上验收：Safari 核对目标和监控、已有报告；仅补缺少的有界演示运行，记录原始结果、节点、CPU/内存、失败及停止证据。
- [x] main 精确暂存提交并推送实现 `5def91a`；更新 CODEX_STATE.md 与证据记录。三份 Word 与 docs/career 保留且未加入提交。
- [ ] 新代码部署版本核验、Safari 新页面与新压测复验。

Safari 已复读两份阶梯旧报告，执行 ID、阈值、阶段和服务资源证据见 `docs/evidence/load-ai-acceptance-2026-09-09.md`。没有新启动压测。直连 SSH 超时、堡垒机终端会话失效，线上部署与新版 Safari 流程仍待恢复通道。

扩大后端全量检查时，首次未使用隔离身份库导致本机 `/opt/midscene-auth` 权限错误，已按仓库门禁配置独立临时身份库重跑；第二次为 1272 passed、2 skipped、1 个旧 AI 超时状态断言失败。旧断言现已同步为 `completed + rule_fallback`，最终包含该双节点端到端测试的压测专项 147 项全部通过。没有把前一次失败计作通过，也没有对生产身份库做修改。

## 2026-09-10 执行进度（覆盖上方 09-09 待部署状态）

- [x] 核验 89befcc 部署、main/远端与静态资源；Safari 实际恢复登录并切换隔离演示工作区。
- [x] 原阶梯并发完整建议/草稿/门禁/运行闭环；规则备用来源明确，达压与4/4阈值通过。
- [x] CPU单接口2次/秒180秒稳态：目标达成、业务通过、P95失败，监控已补齐。
- [x] 2VU人工停止：58请求/14窗口保留、cancelled/105，节点释放、监控补齐。
- [x] 本轮页面真实问题修复53d2c88；Vue645、HTTP75、构建/静态/独立审查完成。
- [ ] 53d2c88部署与Safari实时事件/停止后补取/曲线摘要复验。
- [ ] 专用节点到演示目标TCP路径修复与双节点运行（草稿保留，未发压）。
- [ ] 阶梯吞吐原失败复验、受控错误/保护停止、瓶颈触达/恢复与具体根因实验。
- [ ] Safari重开页面会话pattern异常调查；当前堡垒机终端恢复仍待用户。

三轮新执行与所有未完成边界见 `docs/evidence/load-live-acceptance-2026-09-10.md`；不再使用上方“没有新启动压测”的09-09历史状态描述本次进度。

## 09:30 后补充进度

- [x] Safari 平台终端恢复并部署03bdcea，健康/登录/资源通过，完整曲线摘要已实测。
- [x] Safari 强制重载后会话恢复，CPU和新阶梯最终AI来源/回退原因已补读。
- [x] 现场确认演示机43.157.42.206、平台SSH转发归属和约279ms TCP RTT；健康检查五次对照已记录。
- [x] 修正AI提示词首个JSON示例缺完整workload的矛盾，新增v8，88项AI/策略测试通过。
- [ ] v8部署后真实阶梯重新诊断；新执行实时事件与停止收尾复验。
- [ ] 演示机Safari本机终端、服务本地对照及双节点路径/故障/瓶颈实验。

## 2026-09-10 11:00 实际状态与后续顺序（覆盖历史勾选）

- [x] v8 已随 c98cc3a 部署；真实阶梯 d2d00833 的完整曲线 AI 建议通过参数校验。不能把它等同于自由文本根因和实验都可信。
- [x] 执行历时/累计耗时分开，四类曲线摘要已在 Safari 点检；阶梯并发、CPU稳态、人工停止三轮证据已保存，不重复运行这三轮作为同一版本的新证据。
- [x] 真实设备策略先只用9888E0094F2A，旧设备18CEDF5BA7B2两条写操作基线已移出，新目标v3仅草稿。家用每日08:00任务已核实引用旧目标，现已停用且目标解析阻断；今日08:00历史请求补入审计。
- [ ] 全量设备引用盘点：直接case定时、递归依赖、已排队执行、前置/清理、环境/数据行和动态设备提取。移出baseline不能阻止所有入口。只读审计工具补齐中；生产只读盘点需可用平台服务器会话。
- [ ] 部署807aa0c及后续记录，确认健康版本、JS/CSS资源；11:00:58线上仍为c98cc3a。Safari复验AI完成后自动刷新、原配置复验草稿入口、事件轮询终态补取和停止状态收口。
- [ ] 核对演示机容器实际CPU/内存配额、运行版本和健康本地延迟，与平台隧道健康对照。仅网络RTT/端到端慢不能认定服务CPU瓶颈。
- [ ] 修复专用节点至演示目标的受限转发路径；两节点均连通后用保留的全局2次/秒30秒草稿做双节点实跑，验证分片总量而非每节点重复全局负载。
- [ ] 阶梯吞吐原曲线复验：针对已有失败先确认步骤/连接路径，保留阈值，不能因CPU低直接升压。记录各阶段实际负载与失败/延迟。
- [ ] HTTP错误、业务错误、保护停止：隔离switchable正常预检，启动后在演示机本地分别切换受控故障；每轮记录注入/恢复时刻、触发条件、停止剩余请求、节点进程释放及报告终态。
- [ ] 瓶颈区间和恢复：在路径与配额证据齐全后逐级试探，找到最后稳定/首次持续失败区间，再按CPU节流/4工作槽保护/网络等候选做单变量对照。演示保护上限不等于服务器容量；内存共享64MiB/30秒TTL不能当逐请求累加或泄漏模型。
- [ ] AI架构证据：当前v8仍可能给出与无DB演示服务无关的SQL/连接池或OOM建议。需要区分已确认组件、未知组件、指标缺失及实验授权；不能仅靠参数合法宣布场景化诊断闭环完成。

目前可用平台网页，Safari堡垒机终端已过期至登录页，已请求恢复平台10.130.4.31与演示43.157.42.206会话。新一轮未启动压测或打印，以上未完成项不作通过声明。

## 11:15 部署完成后的续验

- [x] 用户部署后健康接口版本ecff3d3，与远端main一致，包含807aa0c；Safari已重新载入前端。
- [x] 本地AI分类/置信度完整性约束和统一规则回退；158项AI/策略/报告相关检查、8项设备静态审计测试及63项后端静态检查通过。此批尚未部署。
- [x] 只读设备引用审计脚本完成；生产盘点仍待执行，不能把脚本测试当线上引用已清理。
- [x] Safari对现有阶梯d2d00833重新诊断，从诊断中自动变为诊断完成，无手动刷新；报告显示原曲线复验、完整阶段差异与创建入口。
- [ ] 从报告到草稿完整预填复验：报告应用筛选不会自动改变工作台，跨应用进入时正确被阻断；先保存演示工作区再继续。

原多节点路径、演示本地/隧道对照、失败/保护停止、真实瓶颈与恢复、能力事实与逐条AI实验约束继续待办。
