# 录制前景控件与三轮回放验收

## 验收边界

- 手机：PHM110 / `ecbfd645`，Runner：`win-runner-01`，Midscene SDK 1.13.1。
- `9888E0094F2A` 是打印机标识，不是手机。没有发起打印；删除入口只打开确认弹窗并取消，不删除真实业务记录。
- 实际目标页、记录截图、视觉名称、生成 YAML、Runner 报告分别核对。既有旧测试不计入本次三轮。
- 原始默认触控模式存在点击被录到但手机不响应的现象。通过 Sonic 官方“手动修复 → 修复触控”后六步真实导航成功。本次通过结果附带此模式条件，不能宣称原始触控模式已经修复。

## 部署和隔离模型验证

- `2a9c6074fa770ef9c214d37b258d27bc1a866d44` 已通过 Chrome 堡垒机部署，健康接口 release_revision 核对一致，Sonic Web hook 同步。
- 历史会话 `e6f961f5cc5e44d5a03bc8b582406e2e`：显示第5步 `1be7e847b17c45c880a687cb0f89f78e` 的删除弹窗确认按钮实际视觉重识别为“确认”；显示第7步 `f5dd391b3fde4f608fcea9a5eae6c4bb` 首页重识别为“首页”。没有改原截图或坐标。

## 保留的失败诊断

会话 `deebff6e2f944dfcbf632c1f97048bdd` 已结束：默认触控下首次“我的”未跳页，两次“删除”未打开弹窗；识别名虽正确，后端也记录画面无变化警告。该会话不计通过，没有删除失败步骤来制造成功记录。

Chrome 实测还发现桥接将 UI XML 非空当作已识别，以及识别中清空等待序号后下次轮询变绿。`52abbf3` 补修去掉 XML 兜底、持续显示最新步骤识别/失败状态；由已认证的 Sonic 轮询触发有并发上限的后台识别，减少依赖平台后台标签页计时器。64 项后端录制测试、82 项页面/hook 测试、后端63/前端84静态检查通过。该补修部署由用户接管，需健康接口确认后继续验收。

## 第一轮

- 录制：`4de9e0711f374d389d3917d4c410736b`（2a9c607）。六个真实动作：我的 → 打印记录 → 删除 → 取消 → 返回 → 首页；全部视觉识别名称匹配，最终首页实际展示模型推荐内容。
- UI 结束、生成、静态校验、保存：`3D打印基线/录制可见控件验收_20260924_1.yaml`。自动启动仅一个，六组 aiWaitFor（12秒上限）+ aiTap，无 XML 资源 ID/背景日期。
- Runner：`job_1790241112945_00001`，首次执行成功，无自动修复/重试；CLI 58.17 秒。teardown `{ok:true,message:"force-stopped com.kfb.model"}`。
- 报告：`http://101.34.197.12:8088/reports/录制可见控件验收_20260924_1-job_1790241112945_00001.html`。
- 报告包含13个 execution，截图实际核对打印记录列表、删除确认弹窗、取消后列表、最后首页。保存/静态校验没有代替这项核对。
- 录制点选第一条删除，但生成文本“删除”不区分同名行，Runner 实际选择第二条。此次取消无数据变化；如果要精确删除某条记录，仍须补充记录名称/编号或相对行描述，不能把该结果当作特定记录删除验收。

## 第二、第三轮

未完成，待新版发布后的新录制与 Runner 报告。三轮稳定性目前不能宣布通过。

## 删除入口补修

- `48ed196a7b7390ccd8b82de0a0876a35776a7065` 由用户完成部署。2026-09-24 17:36 健康接口确认相同版本，线上 `js/device-recorder.js`、`css/app.css` SHA256 与本地完全一致。
- 每个普通步骤的标题旁常显“删除此步骤”；删除后选相邻步骤并使生成结果失效。同名步骤不自动删除。
- 三项新增回归先红后绿：删除入口被折叠、删除迟到覆盖新会话、旧刷新复活已删步骤；另验证取消不发送删除请求、失败保留步骤并可重试。录制 UI 56 项通过，hook 31 项通过；1366×768、768×480、390×600 删除按钮在截图上方、不超屏。前端静态84、后端静态63、Python/JS语法通过。
- Chrome 验收期间前台窗口被外部切换，已暂停输入并请求独占窗口确认；尚未把源码一致或本地测试当作线上删除验收通过。
