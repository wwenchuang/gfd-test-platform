# 启迪设备 18CEDF5BA7B2 历史指令审计

审计日期：2026-09-10。通过 Safari 只读打开平台历史执行及请求/响应快照，没有重新执行用例、发送设备指令或修改线上基线/定时计划。

## 已确认结论

此前执行的“家用业务基线回归（当前288条）”至少四轮向设备 `18CEDF5BA7B2` 提交了打印与取消请求。四轮均为智小白3D / 家用 / 生产环境（新）-腾讯云，页面来源显示“手动触发”。“手动触发”不能单独证明用户本人操作；这些执行属于此前 Codex 协助完成的基线验收记录。

| 执行 ID | 执行开始（北京时间） | 打印和取消响应 Date 换算北京时间 | 全轮结果 |
| --- | --- | --- | --- |
| `45ac741e-c8b4-4f26-a847-3ad6b0fab999` | 2026-09-09 13:33:53 | 2026-09-09 13:33:54 | 288 通过 |
| `f08274df-b04c-4874-9634-29bf9da879da` | 2026-09-09 14:37:07 | 2026-09-09 14:37:08 | 219 通过、69 中断异常 |
| `0f9c838a-c2d4-47dd-9762-f87225d9c67a` | 2026-09-09 17:36:29 | 2026-09-09 17:36:31 | 287 通过、1 异常 |
| `9caee20a-dfa1-42d8-ac43-ce0b6486d21d` | 2026-09-09 17:48:59 | 2026-09-09 17:49:00 | 288 通过 |

时间来自各用例保存的服务端响应 `Date`（GMT + 8），不是设备实际启动/停止时间。每轮两条请求均逐条查看，合计已确认 4 次打印请求、4 次取消请求。中断轮的两条请求发生于 Worker 中断之前，不能归因为恢复机制自动重放。

## 请求与断言

“打印任务下发 - 基础正向流程”：`POST /app/pmc/api/v1/deviceCmd/print`，主机 `print.wisebeginner3d.com`。四轮保存的请求体相同：

```json
{
  "url": "https://cdn-h.gongfudou.com/three-dimension/qidi.gcode",
  "printSn": "323456345",
  "deviceId": "18CEDF5BA7B2",
  "fileType": "gcode"
}
```

“任务指令下发 - 基础正向流程”：`POST /app/pmc/api/v1/deviceCmd/send`，同一主机。四轮请求体相同：

```json
{
  "cmdCode": "cancel",
  "deviceId": "18CEDF5BA7B2",
  "requestId": "3234564126"
}
```

URL query 中的 deviceId 是示例值 `QD1122334455`，但实际保存的 body 指向上述启迪设备，不能只检查 URL 就认定目标为模拟设备。此记录省略认证参数和请求头。

八条请求均保存 HTTP 200 与 `{"code":0,"msg":"success","data":true}`。页面仅有 HTTP 状态为 200、业务 code 为 0 两条断言，没有设备回执终态、实际取消成功或恢复空闲的验证。因此原 288/288 只证明已配置接口断言通过，不能证明真实设备安全、打印完成或取消清理成功。

## 与用户截图的对应边界

截图设备号、重复打印 request_id `323456345` 和取消 request_id `3234564126` 与上述请求一致。截图有四组 print/cancel，print 显示 ok、cancel 显示 fail；但截图没有时间列，尚不能把平台四轮与数据库行 ID 逐行严格对应。

截图另有 `91986139-5612-4b35-b75c-aea3d5ddcfe7` 的打印记录，本次未找到来源，不能归因给这四轮。取消 requestId 与 printSn 不同本身也不足以证明取消参数错误，需要业务接口语义与设备回执。

仍缺数据库记录时间、调用方/操作者、设备回执错误详情和 UUID 请求来源。现有证据足以确认平台曾发出这些指令，不足以认定用户本人手动操作，或计算设备真正启动打印次数。

## 验收修正与后续

- 暂不再次执行包含这两条用例的原 288 条基线。应先盘点这两条用例及其他设备写操作的基线/任务/定时引用，明确设备授权与运行前状态条件。
- 将真实设备动作从日常通用回归中隔离，绑定明确的测试设备与一次运行的标识；取消与恢复需要设备终态证据，不能仅凭接口受理成功通过。此项为待实施方案，本次未修改线上资产。
- 当前演示服务压测目标为隔离演示机 `43.157.42.206`，本次三轮演示压测不等于上述家用基线；本次审计也没有新启动任何打印或压测。
- CODEX_STATE 中原“稳定性问题已关闭”仅适用于 Worker 中断收敛与图生图外部图片超时问题；新增的真实设备动作及验收门禁缺口尚未关闭。

历史页面入口：`http://101.34.197.12:8088/api-test/?from=login#/runs?executionId=<执行ID>`，登录后查看对应两条用例。

## 后续补查：2026-09-10 自动调度与停用

- 用户随后明确指定后续先只使用 `9888E0094F2A`。两条旧v2正式基线已移出有效集合，新目标v3仅保存草稿，未执行、未采纳；固定工作规则已提交 `ecff3d3`。
- Safari切换并保存3D家用查看范围后，定时任务实际为2条；此前演示项目的0条不代表家用没有定时任务。家用任务每天08:00、多条基线、允许一次性基线，原配置已启用。两条基线移出后，页面明确显示“执行已阻断 · 2个目标已删除或不属于当前项目”，手动执行按钮禁用。
- **补充第五组已确认请求**：自动调度执行 `ffd40cf0-98fc-44d3-92ba-96c0399d8e57` 于2026-09-10 08:00:24开始，288/288，执行历时225.79秒/累计220.81秒。逐条读打印和取消快照，仍为旧设备 `18CEDF5BA7B2`、打印号 `323456345`、取消号 `3234564126`；两条服务端Date均为Thu,10Sep2026 00:00:25GMT，即北京时间08:00:25。HTTP200/code0/data true，仍只有两条接口断言。至此已确认至少5组，不是历史总量穷尽。
- 已在Safari关闭“家用业务基线回归（当前288条）”启用开关；页面显示停用成功、配置已停用、目标阻断仍在。保留任务配置、通知偏好、历史执行与共享任务。未发出新设备指令、未触发定时任务手动执行。
- 用户指定单设备的全量引用盘点尚待：直接case/task、依赖的case_version_id、前置/清理与环境间接变量。移出两条baseline不能当作所有入口都有设备拦截。完成盘点和合规目标修正前不恢复家用全量定时执行。

## 只读全量盘点工具（待生产执行）

新增 `scripts/audit_api_device_references.py`。在平台已配置数据库的应用环境运行，不另抄凭据：

```bash
python3 scripts/audit_api_device_references.py --project-id 01c72b31-a9be-4aab-8375-b054f33b826c --output /tmp/home-device-reference-audit-20260910.jsonl --allow 9888E0094F2A --block 18CEDF5BA7B2
```

输出必须为新文件；权限0600，拒绝覆盖。SQL事务只读、可重复读，查询限时30秒、锁等待3秒；不发HTTP、不执行用例、不解密秘密变量、不发送通知。无效项目直接失败，合法空项目也输出16类数量，不能把查错项目当无引用。

覆盖所有case与环境版本（含历史）、数据行、脚本、提取、断言、依赖闭包、基线、任务、定时和QUEUED/RUNNING执行。输出路径及关联ID供人工追踪；动态设备选择与秘密变量明确标记待审，不宣称静态分析证明运行安全。不覆盖终态历史执行的全量溯源或性能场景/性能定时。

本地8项测试通过，包括实际ORM模型的PostgreSQL语句编译、SQL层秘密值置空、归档baseline与直接case版本引用差异、递归依赖、未知项目和零数量。生产数据库尚未执行，不能据此宣布旧设备所有入口已清零。

## 后续生产只读盘点与三条遗漏基线隔离

在Safari平台机10.130.4.31终端，以已配置应用环境实际执行脚本，生成 `/tmp/home-device-reference-audit-20260910.jsonl`（只读事务，不输出秘密）。本节覆盖上方“生产尚未执行”。清单：1116个case、2671个version、89个data row、1个script、51个extraction、6503个assertion、548个baseline、2个environment、33个environment revision、178个variable、303个service、2个schedule/298个target、14个task；QUEUED/RUNNING执行与其用例均为0。

7条baseline有旧设备结构关联，其中3条active。Safari按精确名称核对后逐条移出，保留case/version和历史：

| baseline ID | case version ID | 用例 | 页面接口 |
| --- | --- | --- | --- |
| aeb6c26d-37cd-432b-b69a-4d2578b322df | 76c4c02c-9e00-42de-8cb4-b620b3395642 | 固件更新接口 - 基础正向流程 - 响应数据驱动 | GET /pmc/api/v1/iot/ota/upgrade-info |
| 6a7726ef-29d8-4f92-a0e3-d9bf22503293 | b0dedbf0-aecd-408e-b25c-2bb52839fb19 | 设备默认用户信息 - 基础正向流程 | GET /print3d/api/v1/qidi/user |
| 61fbb02b-3486-49a0-9b19-3d9181bb7d4a | 1618b1e5-a0ea-4731-ab2e-a4717c21c842 | 我的模型收藏 - 基础正向流程 - 响应数据驱动2 - 真实响应数据联动补跑 | POST /print3d/api/v1/qidi/collection/page |

有效数299→298→297→296，页面逐条确认已移出。第三方API名称不能代替语义，固件信息GET不应被描述为这次发送固件升级命令；本次没有执行这些接口。两条定时仍为家用DISABLED且目标解析阻断、共享ENABLED。

清单还含30处旧设备字面引用、777处动态引用、750个设备字段待审标记、543个提取待审标记、29个秘密值未读取。它们含历史、重复路径及静态候选，不能把标记数量当独立用例数，也不能把隔离3条active解释为全部动态设备选择已受平台运行门禁约束。审计后完整清单复核继续保留为待办，设备相关执行不恢复。

第二次生产只读盘点已执行，另存 `/tmp/home-device-reference-audit-20260910-after.jsonl`，保留原文件。Safari终端汇总实读：`active_baselines 296`、`active_blocked []`、`pending=0`、`pending_cases=0`。这关闭的是静态关联旧设备的有效基线遗漏；动态选择、秘密变量、直接运行历史版本等边界仍如上，家用定时不恢复。
