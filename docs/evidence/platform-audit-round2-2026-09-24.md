# 全平台审查第二轮：业务验证与修复证据

## 范围与部署基线

2026-09-24 用户要求扩大到整个平台，不能只检查录制。按既有全平台矩阵审查任务生命周期、Agent 门禁、资产和版本、权限、API 环境/用例/调试/报告、通知配置、压测分配、布局及性能。采用代码复现、隔离数据库回归、Chrome 业务操作三类证据，分别标明结论。

线上健康接口确认已部署 `ad82912189cabe74380db1e28667c0460ed12cbe`；Windows Runner 上报 `2026.09.24-midscene1.13-qwen3.7-result-retry-v1-recording-preparation-v4`。以下新增代码由用户部署后再做线上复验，本轮没有重启或部署生产服务。

## 本轮确认并修复的问题

| 编号 | 缺陷、原因与改动 | 回归证据 |
| --- | --- | --- |
| B01 / P1 | 任务回传/修复先读默认最近 50 条，再覆盖整个任务文件，历史可能丢失。所有相关读改写路径读取完整列表 | 51 条任务回传后仍为 51 条；修复不能恢复此前已经丢失的历史 |
| B02 / P1 | 已取消任务被迟到的 Runner/Sonic 成功回包改回成功；加写日志前和持锁更新前的取消检查 | 已取消回包 ignored，不更新元信息、不触发后续动作；报告写入期间取消保留取消终态 |
| B03 / P1 | Agent 取消依据旧快照，可能覆盖刚完成的子任务；锁内重读并检查终态和父任务 | 回归模拟读取快照后子任务成功，取消不覆盖成功 |
| B04 / P2 | “取消成功”暗示真机动作已停止；响应和页面明确区分取消排队/后续步骤与已开始执行的动作 | 两个页面显示服务端执行能力提示。尚未实现正在运行的 Windows 进程中断 |
| B05 / P2 | 同一秒保存多次使用相同版本 ID，旧备份被覆盖；加入纳秒及随机后缀 | 连续保存两份不同内容均可独立读取恢复 |
| B06 / P2 | 批量移动部分成功返回 207，页面未刷新仍选中已移动源文件 | 强制刷新，只保留尚在源模块的文件供重试，并提示部分成功 |
| B07 / P1 | API 环境创建与密钥保存分两次请求，第二次失败留下半成品，重试名称冲突 | 改为同一事务创建环境/初版/加密密钥；PostgreSQL 验证回滚无残留、明文不出现在公开变量和兼容旧导入 |
| B08 / P1 | 快速切换项目/活动归档/环境详情，迟到结果覆盖当前列表和详情 | 请求代次、选择身份校验；清空/新建时使旧详情与历史请求失效 |
| B09 / P2 | 项目通知配置迟到显示到另一项目；保存和读取交错后保存按钮一直忙 | 配置绑定项目、响应代次；保存忙状态单独计数。未发送真实通知 |
| B10 / P2 | 用例管理读取接口列表失败却显示为空，没有解释 | 展示接口资源加载错误；503 分支测试 |
| B11 / P2 | 压测服务的校准检查与分配器使用不同时间来源，注入时间时出现虚假容量不足 | 统一传递 now；原数据库 E2E 由失败转为通过，未把此测试场景说成线上容量事故 |
| B12 / P2 | 全局顶部工具栏在1180/720断点固定最小150/246像素，动作隐藏时仍大片空白压缩业务内容 | 删除两处固定断点高度，保留基础64像素及换行；真实浏览器验证内容与按钮边界 |

此外，主平台视觉测试仍从已改变用途的设备提示栏读取应用名称；改为验证实际手机卡片的应用名称、版本和 Runner，保留原测试意图。

## Chrome 实际业务验收（线上 ad82912）

### Agent 与 YAML 资产

- 输入临时目标，选择智小白3D/家用/Android 安全自动，预览计划；核对应用包名 `com.kfb.model` 与目标。未启动真实 Agent 运行。
- 新建 `AI测试/平台验收临时_20260924_1455.yaml`，保存 launch、aiWaitFor、aiAssert，离页重开内容一致。
- 历史预览只显示历史内容，不覆盖当前编辑器。复制为副本，移动到 `3D打印基线` 后列表/编辑路径正确。
- 筛选自己创建的两份 YAML，批量删除；列表为空，API 复查两个实际路径均为 404。

### API 环境与真实接口调试

只使用已有“压测演示（非业务）”项目和隔离演示服务的 `GET /demo/ok`，没有打印、删除业务记录或调用业务写接口。

- 新建 `平台验收临时_20260924_1455` 环境，保存 v1；改描述生成 v2；从历史恢复 v1 生成 v3，原版本保留。随后归档。
- 临时用例 `平台验收临时_20260924_1455_正常响应` 保存并调试，HTTP 200/业务码 0/断言通过，Chrome 显示 484ms。执行 ID `f498f10c-53d7-4c31-8228-3a527bf5a046`，服务端 summary passed=1。
- 将期望状态码故意改为 201，同一 GET 实际返回 200；Chrome 明确“HTTP 状态码 期望 201，实际 200”，711ms。执行 ID `8335f4dd-49a7-4b88-a06f-5a5713d3fced`，summary failed=1，不误报成功。
- 没有采纳为正式基线，没有创建定时任务。API 复查确认执行结果，完成后归档本轮用例/两份执行记录，删除本轮自动创建的临时任务；活动用例/执行/任务列表均为 0，原隔离演示环境仍保留。

清理 ID：用例 `b7bfca38-5a95-4ea0-93b2-0556d13ed4d2`，任务 `337d6f53-26e3-4dc4-9fb9-3b6f3f12ab95`，环境 `ada156f1-6f28-402d-9395-794b95fcb141`。归档是产品正常保留历史方式，未直接清库。

## 性能与读取检查

- 21 个核心读取 API 本轮全部返回成功，覆盖认证、应用/模块、Runner、Agent、任务、报告、失败处理、知识、录制、模型等；读取成功不代表对应写业务全部通过。
- `/jobs?history_limit=10&background_limit=10` 样本 2.042s；默认 `/jobs` 2.700s，上轮约 22.8–25.4s。当前两个样本说明已部署的索引复用有效，不当作 p95 或长时稳定性结论。
- 本轮未做生产压测。完整 API 测试使用本地 PostgreSQL 随机 schema 和独立临时 Redis 容器，不清理用户已有 Redis。

## 检查结果

- API 后端全套：1385 passed、2 skipped、0 failed，196.29s。两个跳过项是缺少本机 k6 的真实进程迭代清理测试，不算通过；其余含 PostgreSQL、Redis、HTTP、权限、压测 E2E。
- API 前端：92 文件、699 测试通过；Vue 类型检查及 Vite 构建通过，提交重新生成的 api-test 静态资源。
- 主平台权限/应用归属/Agent 确认与需求边界/审查修复/性能/设备引用共 118 项，分文件隔离运行通过；新增生命周期及版本测试 8 项通过。
- 后端静态 63、前端静态 84、主链及修改文件 Python 编译通过。主平台 Node/视觉最终结果见本文件末尾。

## 尚未完成的验收边界

1. 新补丁尚未部署，不能把 Chrome 对 ad82912 的业务验证称作本批并发缺陷在线验收。尤其环境原子保存必须前后端一起更新。
2. 当前取消保证平台终态、排队与 Agent 后续步骤，不保证中断已运行的 Windows Runner 设备动作。需独立补充 Runner 停止协议和实机终态验证；本批没有用“提示已取消”冒充停止。
3. 本轮没有新增完整 Agent 生成→冒烟→报告的真机执行，也没有新增录制/回放长期稳定性证据。PHM110 当时有现存远控占用，未夺取会话。手机序列号 `ecbfd645` 与打印机 MAC `9888E0094F2A` 分开。
4. 权限矩阵有本地回归，但没有在线切换不同真实用户验收。通知只验证配置逻辑，没有给群发送测试消息。
5. 低优先级 API 一致性待明确：上传新资产时显式提供新的 case_set_id 会被当作已有对象检查归属；不提供 ID 正常。当前普通前端未使用该入口，未放宽所有权校验。
6. 若取消恰好发生在报告文件写入后、状态保存前，可能保留未关联的报告文件；平台终态与后续处理已保护，本批不自动清理可能关联用户任务的报告。

## 部署与复验顺序

由用户在平台源码目录执行：

```bash
cd /opt/midscene-task-platform-src
bash deploy/update-main-server.sh
```

本轮新增补丁只涉及平台后端、页面和构建资源，无需再次替换 Windows Runner 或重建 Sonic Agent。核对最终 revision 和健康接口，Chrome 刷新后先验资产保存/版本/批量操作、环境新建与切换，再进行受控任务回传与取消验收。此前已更新的 Runner 继续保留。

## 最终复核结果

- `VISUAL_ARTIFACTS_DIR=/tmp/platform-round2-visual-complete node --test --test-concurrency=1 tests/*_check.js`：238 passed、0 failed，73.71s。覆盖主平台 Node、身份权限浏览器验证、主平台视觉及 API UI 视觉。浏览器测试串行运行，避免争用影响交互时序。
- 独立完整主平台视觉、API 视觉均通过；人工查看1110×629、390×844截图确认工具栏空白确实缩小。主平台工具栏从150/246像素降为约82/80像素；可见动作仍能换行且在工具栏边界内。
- 修正三处测试问题：应用名称读取位置过时；同名“关闭导航”误点遮罩被覆盖的中心（现验证真实关闭按钮与露出遮罩）；已登录跳转过程中 evaluate 登录页函数偶发上下文销毁（现等待并断言真实目标URL）。均未强制点击或放宽产品正确性断言。
- 最后 Python 编译、后端静态63、前端静态84、Vue类型/Vite构建、JS语法、`git diff --check`通过。
