# 2026-09-09 压测设计与验收续接证据

## 版本与已完成事项

本轮开始时，本地 main、远端 main 与线上 `/api/health.release_revision` 均为 `dfa1126a36235c6be58f635a9e3760409be174a6`。18:48 再次读取线上健康检查仍为该版本。本批新代码尚未部署。

Worker 恢复 `438fbac` 与家用基线 v4 采纳沿用已有证据；最终执行 `9caee20a-dfa1-42d8-ac43-ce0b6486d21d` 的 288/288 已完成，本轮没有重复执行。

## Safari 线上复读

本轮实际使用 Safari 打开性能报告，切换到“压测演示（非业务）”历史，读取以下两份终态报告及已完成的 v6 AI 分析。

| 项目 | 阶梯并发 | 阶梯吞吐 |
| --- | --- | --- |
| 执行 ID | `2e6407f4-aafd-4f2f-8e05-e230b7b33b75` | `183d044c-b3e6-4149-8827-d909e2cfa58d` |
| 曲线 | 起点 1 VU，2→4→1，每段 15 秒 | 起点 1 次/秒，2→4→2，每段 15 秒 |
| 请求 / 完整链路 | 117 / 39 | 330 / 110 |
| HTTP / 业务 / 链路失败率 | 0 / 0 / 0 | 0.30% / 0.30% / 0.91% |
| P95 | 1000 ms | 1825.316 ms |
| 阈值 | 4/4 通过 | 1/4 通过，P95 要求 1200 ms |
| 目标达成 | 各阶段未证明达成 | 各阶段合计未达成；计划 22.5、45、45，实际 22、43、45 |
| 实际节点 | 本机（备用），单节点 | 本机（备用），单节点 |
| 压力机运行资源 | 10 样本，CPU 0.17 核、内存 106.84 MiB | 10 样本，CPU 0.19 核、内存 108.53 MiB |
| 服务资源 | CPU 峰值 0.06 核、内存 91.66 MiB，各 17 样本 | CPU 峰值 0.11 核、内存 79.23 MiB，各 17 样本 |

并发轮先核对阶段 VU 轨迹与达成判定，保留原曲线复验。吞吐轮先定位慢/失败步骤，再按原曲线复验。低 CPU 与短阶段数据均不能证明还有多少容量。

## 目标与监控归属

仓库部署记录中的演示目标为 `43.157.42.206`，平台为 `101.34.197.12`；`106.54.190.138` 是历史证据中的平台实际出网 NAT，不能当作演示目标地址。

Safari 报告确认监控版本 `f411ab83-ce40-413a-a851-3ec395e47c88`，服务名称“演示服务自身容器”，Prometheus 来源 `http://172.22.0.1:19090`；标签包含 `id=/load-demo`、`job=load-demo-self-cgroup`、`instance=demo:8080`、`container=load-demo` 和 `purpose=controlled-demo-not-business`。这是演示容器自身 cgroup 指标，采用兼容的容器指标模板，并不表示安装了独立 cAdvisor。

`172.22.0.1:18080/19090` 是 Agent 侧隧道入口。以上报告证明了监控版本和样本归属；本轮未能进入服务器独立核对现有隧道进程、目标容器配额和两台节点的实时路径，不能标记为完成了全链路服务器复核。

## 新代码范围

- 执行历时按起止时间计算；用例累计耗时独立求和，缺失不互相替代。
- v7 AI 完整阶段建议、结构纠正一次、门禁校验、保留原提案和调整原因、备用建议状态。
- 报告展示完整曲线差异和九类瓶颈证据覆盖；下一轮草稿绑定原执行/诊断，保留环境、场景、监控、阈值和停止条件。
- PostgreSQL 与 Vue 回归覆盖四种负载契约、失效提案、失败/缺证据优先级、资源真实标签、观察时间、阶段单变量和历史缺字段兼容。

## 未完成与恢复入口

新代码部署、Safari 新版完整链路、演示双节点、故障切换、人工/错误保护停止以及足够长稳态的瓶颈实验均未完成。本轮没有启动新压测，没有改变线上配置，没有发送通知。

部署通道阻断：直连平台 SSH 22 端口超时；Safari 堡垒机终端会话失效，未取得可用 shell。恢复平台服务器终端后，在 `/opt/midscene-task-platform-src` 按既有 `bash deploy/update-main-server.sh` 部署 main，并通过健康检查确认实际版本，再执行上述缺失验收。不要把 main 推送成功等同线上发布成功。

根因诊断目标与后续交付顺序见 [AI 完整流程设计](../superpowers/specs/2026-09-09-load-ai-closed-loop-design.md)。GC、连接池、SQL 锁等待和 trace 尚未接入；本批九类清单是证据覆盖，不是所有瓶颈已定位。
