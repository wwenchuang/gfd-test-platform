# 3D API 核心链路执行记录（2026-09-03）

## 结论

本轮从 Apifox `默认模块 v11` 的 1031 个接口中整理出 12 条可独立复验的核心链路用例，并全部形成当前版本基线。所有 12 条基线统一使用 `核心链路` 分组，并统一放入 `3D核心链路回归（v11）` 任务。

- Safari 从任务管理页真实点击执行，`117f0e97-1a43-4da4-82fe-3f2e14896b67` 通过 12/12、通过率 100%，产品失败、脚本/数据、环境异常均为 0，耗时 122.16 秒。
- 飞书通知发送成功，任务名称为 `3D核心链路回归（v11）`。
- 新增的模型导入和中文搜索链路均先连续调试两次，再采纳基线；真实打印链路也已完成两次调试和 Safari 任务执行。

每日 08:00 的家用 290 条、共享 10 条调度本轮未修改。当前活动基线为 303 条，其中家用 293 条、共享 10 条；核心任务是便于快速判断主功能的 12 条集合，不能用它替代其他非核心接口的覆盖结论。

## 核心链路树

| 层级 | 核心链路 | 业务步骤 | 清理边界 | 状态 |
| --- | --- | --- | --- | --- |
| 家用稳定 | 账户会话与用户资料 | 用户详情与身份字段 | 只读 | 已通过并采纳 |
| 家用稳定 | 模型浏览与收藏闭环 | 当前设备类型 -> 模型列表 -> 模型详情 -> 收藏 -> 收藏状态/列表 | 取消本次收藏并确认恢复 | 已重复通过并采纳 |
| 家用稳定 | 设备状态与耗材能力 | 设备列表 -> 首页设备 -> 耗材 -> 在线状态 | 只读；断言空闲状态 | 已通过并采纳 |
| 家用稳定 | 打印历史与详情查询 | 当前设备 -> 打印历史 -> 切片任务查询 -> 打印详情 | 只读 | 已通过并采纳 |
| 家用稳定 | 下载记录创建查询清理 | 我的作品 -> 创建下载 -> 详情 -> 列表 | 删除本次下载记录 | 已重复通过并采纳 |
| 家用稳定 | 学习冒险地图与任务详情 | 当前冒险 -> 世界地图 -> 任务详情 | 只读 | 已通过并采纳 |
| 共享稳定 | 共享地区级联查询 | 省 -> 市 -> 区 -> 经纬度地址 | 只读 | 已通过并采纳 |
| 共享稳定 | 共享登录滑块契约 | 生成验证码 -> 无效滑块校验 | 不建立登录会话 | 已通过并采纳 |
| 家用核心 | DeepSeek CAD 创建查询删除 | 创建 -> 轮询成功 -> 产物断言 -> 改名 -> 详情/列表可发现 | 删除本次任务并确认列表为 0 | 三次通过并采纳 |
| 家用硬件 | 真实打印创建查询取消 | 指定空闲打印机 -> 校验历史切片详情 -> 创建重打 -> 查询 -> 取消 | 确认任务取消、设备恢复空闲并删除本次记录 | 两次调试及两次 Safari 任务执行通过 |
| 家用核心 | CAD 产物导入处理与清理 | 导入 OBJ/MTL -> 轮询处理完成 -> 我的模型详情 -> 定位数据库 ID | 按数据库 ID 删除并确认“模型不存在” | 连续两次调试及统一任务通过 |
| 家用核心 | 中文搜索结果与模型详情 | 读取设备类型 -> 中文关键词搜索 -> 取得结果 SN -> 打开同一模型详情 | 只读 | 连续两次调试及统一任务通过 |

## 可复验证据

| 对象 | ID | 原始结果 |
| --- | --- | --- |
| 统一核心任务 | `2975548b-8fa8-43fd-b92f-3b019ca17211` | 12 条可执行基线，全部属于`核心链路`分组 |
| 统一核心 Safari 执行 | `117f0e97-1a43-4da4-82fe-3f2e14896b67` | 12/12，通过，122.16 秒；飞书发送成功 |
| 稳定核心 API 执行 | `39252585-904c-4065-82e5-37bbf9e79f15` | 8/8，通过；飞书发送成功 |
| 稳定核心 Safari 执行 | `bc99d003-7137-47a5-be46-a7cf3f1aa0ad` | 8/8，通过，4.52 秒 |
| 共享核心任务 | `6d4b03a9-f437-4721-ba49-5ba667b3b74e` | 2 条可执行基线 |
| 共享核心执行 | `1e766f8a-f2c4-4c67-a9f8-131ca0fb2a6f` | 2/2，通过 |
| CAD 扩展用例 v3 | `2ee68542-3050-4f06-9f97-7ef6783b494c` | 校验 0 错误、0 警告 |
| CAD 连续调试 | `f4966182-dbf0-48c7-b350-e76de4e2ad54`、`ad9ea292-9352-4bcd-804a-3e7e6a05a1c5` | 各 1/1，通过且清理成功 |
| CAD 扩展任务 | `4e6e1120-2278-4082-a180-6b59f641f341` | 1 条按需基线 |
| CAD Safari 执行 | `c2189c08-4f29-4083-8c63-0ed5e4d05154` | 1/1，通过，32.87 秒，13 段执行证据 |
| 真实打印用例 v2 | `c2d727eb-3de0-4abf-99ca-7f26fc369f29` | 两次调试通过；基线`6ad4b223-fb31-4cf8-82cb-2c74b79368ac` |
| 真实打印 Safari 执行 | `c80e84b7-4931-4474-aff1-65ce65f88337` | 1/1，通过；指定设备创建、取消、清理完成 |
| 导入用例 v2 | `471eb365-a361-421d-ba3c-3c2c75ff45b8` | `dc7b8861...`、`08d57071...`连续两次通过；基线`f1596faf...` |
| 中文搜索用例 | `44d9550e-2f91-47e5-b530-2d8cec6af30e` | `17b25df7...`、`106906ef...`连续两次通过；基线`63cf8c5e...` |

Safari 已核对统一任务 12/12 的范围并实际点击生产确认、执行、实时轨迹、用例明细、测试报告及全部状态筛选。新增导入与中文搜索用例的每个步骤、长响应展开入口和完整轨迹入口均已点击；认证信息显示为已隐藏。

## Apifox 契约与真实接口差异

CAD 首版暴露了三个必须按真实响应修正的问题：

1. 创建接口真实返回的 `data` 是整数任务 ID，不是文档假设的 `data.id` 对象。旧提取会在真实任务已创建后失败。
2. 改名接口实际限制任务名最多 10 个字，超限返回 HTTP 200、业务码 1002，不能按传输成功处理。
3. 任务处于 `PROCESSING` 时删除返回 HTTP 200、业务码 1001。v3 采用有上限的详情轮询，只有状态为 `SUCCESS` 且 OBJ、预览图存在后才改名和删除。

失败版本产生的两个调试任务均已精确删除。v3 每次运行还会用新名称查询列表，删除后要求该名称的列表总数为 0，因此没有把清理结果建立在删除按钮或 HTTP 200 上。

模型导入链路另发现 `/createModel/delete` 的 `ids` 实际要求数据库 ID。首次错误传入模型 SN 时接口仍返回 HTTP 200、业务码 0，但详情仍可查询。通过 `/createModel/page` 定位到首次产物数据库 ID `2111033634` 后已精确补删；v2 会先断言列表首条 SN 是本轮导入值、提取数据库 ID，再删除并要求详情返回业务码 1001、`模型不存在`、`data=null`。统一任务本轮产物 SN `1545035073063321600`、数据库 ID `2111033646` 已按此流程清理。

## 已移出范围及仍需外部数据的链路

| 链路 | 已做的动态发现 | 需要的条件 |
| --- | --- | --- |
| 京东之家默认切片与共享设备 | 当前绑定 EP7、当前模型及组件均能动态取得，但两个京东之家接口都返回业务码 1001、`404:null` | 用户明确本轮不做；不建用例、不进入核心任务、不再索取数据 |
| EPONEPro Camera 会话 | 当前账户只能取得 EP7，不能据此伪造 Camera 能力 | 用户明确 EPOne/EPONEPro 本轮不做；不建用例、不进入核心任务、不再索取数据 |
| EPOne 设置原值回写 | 使用当前设备调用同步的 v11 路径返回 HTTP 404 `Not Found` | 用户明确本轮不做；保留接口路由异常记录，不再推进或索取设备数据 |
| 真实打印创建查询取消 | 用户提供 Mac `9888E0094F2A`、SN `1385270626257522688` 并授权创建后立即取消 | 已完成；每次运行前后均要求 `ready / standingBy`，统一任务后 `printingFlag=false` |
| 设备首次绑定/解绑 | 未执行 | 用户明确不考虑，不进入核心链路 |
| 共享商城下单支付 | 未执行 | 用户明确不考虑，不进入核心链路；只保留已跑通的共享只读契约 |

京东之家、EPOne/EPONEPro、设备首次绑定和共享商城完整交易均按用户决定移出范围。自动修复与失败重跑候选继续留在修复区，不进入这套核心基线。

## 体验与运行状态

- 任务卡能直接看出接口数、可执行用例数、应用/业务、环境和最近结论；生产环境执行前有明确确认。
- 稳定执行报告显示产品失败、脚本/数据和环境异常均为 0；收藏用例的添加、验证、取消和恢复证据均可逐段查看。
- CAD 报告把 6 次 `PROCESSING` 轮询和第 7 次成功分别展示，便于区分等待与失败。
- Safari 首次进入执行记录曾出现约 22 秒加载；随后对同一 50 条列表连续三次请求为 367、2174、1578 毫秒，未在后端稳定复现。该项作为冷启动或前端渲染性能问题继续观察，不能写成已经修复。
- 登录成功从主平台返回裸 `/api-test/#/...` 时，Safari 再次命中 nginx 404；手动给入口增加查询参数即可进入。代码已在 `js/auth.js` 为 API 登录回跳增加独立缓存键，并补了浏览器目标回归，仍需部署后复验。
- 完成后健康接口版本为`f4012f905be795a0c861e51c761804a7150ace4a`，PyYAML 可用、无活动大请求或重任务，RSS与峰值均为232.0 MiB。
