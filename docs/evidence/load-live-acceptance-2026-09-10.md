# 2026-09-10 部署后 Safari 压测验收

## 范围与版本

- 用户确认部署后继续真实压测。已重新读取 AGENTS.md、CODEX_STATE.md 的 Worker 恢复记录及已有验收计划；没有重跑已完成的 288/288 家用基线。
- 本轮开始，本地 main、远端 main 与线上 `/api/health.release_revision` 一致：`89befcce082858675bf43761906c6d8c9edb8acf`。09:05:51 再查仍为该版本。
- 初始线上 `/api-test/` 与本地已部署构建逐字节一致；入口 JS `index-a-zcHRcW.js` SHA-256 前缀 `d96bf2bf1dde2d9b`，CSS `index-P_MWXx87.css` 前缀 `a15eb3dbfc993501`。后续本地修复重新构建的文件不代表已经上线。
- 用 Safari 原生页面切换到“压测演示（非业务）”，环境“隔离演示环境 · v4”，只访问受控演示接口。通知关闭，未发送消息、未修改节点容量、未放宽失败阈值。
- 三份 Word 和 `docs/career/` 保留，未纳入提交。

## 目标与监控边界

- 平台入口：`101.34.197.12:8088`；历史演示目标：`43.157.42.206`。09:20 后 Safari 平台终端已恢复，SSH 隧道实际目标现场核实为 43.157.42.206；用户实例截图确认 2 核 / 2 GB。演示机本机终端和实际容器配额仍待复核。
- 当前环境 API 地址：`http://172.22.0.1:18080`。Prometheus：`http://172.22.0.1:19090`。
- 监控版本：`f411ab83-ce40-413a-a851-3ec395e47c88`，标签 `id=/load-demo`、`job=load-demo-self-cgroup`、`instance=demo:8080`、`purpose=controlled-demo-not-business`。指标来自演示容器自身 cgroup，采用兼容指标名，不能误写成已安装 cAdvisor 或整个业务服务资源。
- CPU 为两分钟 rate；内存为 working set。新 CPU 稳态保持 180 秒，前后观察各 60 秒，等待后窗口和数据回补完成才记录最终报告。未将发压 Agent 的 CPU/内存当成服务资源。
- 两台 Agent 均为 0.1.5、k6 0.52.0、心跳正常且校准有效。09:02:56 核对，两台均为 0 进程、0 VU。

## 本轮实际运行

| 执行 | 参数与目的 | 终态证据 | 结论 |
| --- | --- | --- | --- |
| `d2d00833-843e-44fb-89b8-5e4d99edd890` | 原完整链路阶梯并发复验；起点 1 VU → 2 → 4 → 1，每阶段 15 秒，总 45 秒；本机单节点 | 96 HTTP / 32 完整链路；三类失败率均 0；P95 1187.36 ms ≤ 1200；目标达到，4/4 阈值通过；观测历时 50.316975 秒 | 监控补齐后 `passed`；只证明本次曲线通过，不证明容量上限 |
| `19893aba-801c-4ad6-81df-6e3bbbcbf353` | CPU 单接口稳态；2 次/秒，180 秒，预分配 2 / 最大 4 VU；本机单节点 | 360 HTTP / 360 链路；实际 2 次/秒，目标 100%；三类失败率均 0；P95 1142.547 ms > 500；3/4 阈值通过；观测历时 183.476842 秒 | 监控补齐后 `failed`，响应时间不达标 |
| `841b9d0d-6161-4922-9bb9-313fe0de57b5` | CPU 单接口人工停止；2 VU、计划 120 秒，约 30 秒主动停止 | 58 请求；实际并发达标采样 30.999 秒，观测历时 34.957492 秒；14 指标窗口；节点 `cancelled`、退出码 105，00:58:06Z 日志确认收到 interrupt；P95 1103.678 ms | 停止与证据保留验收通过；性能报告 `inconclusive`，不能把未完成的 120 秒观察标为通过 |

第一轮资源读取：服务 CPU 峰值约 0.05 核、内存 80.04 MiB；发压节点 10 个样本，CPU 0.18 核、内存 110.02 MiB。09:33 Safari 再读最终报告，CPU/内存有效样本均为 17。

CPU 稳态最终服务资源：CPU 峰值 0.22 核、working set 峰值 25.75 MiB，两项各 61 样本。发压节点 36 样本，CPU 0.14 核、内存 85.07 MiB、CPU 配额占比峰值 4.58%。

停止轮监控已完成：服务 CPU 0.07 核、内存 28.32 MiB，各 32 样本；发压节点 7 样本，CPU 0.14 核、内存 92.52 MiB、配额占比峰值 4.74%。观察窗口无并行压测污染。

新增场景 `Demo CPU isolation 20260910`：场景 ID `de374014-e20a-4715-92d6-189c3063f023`，版本 `6b5a56b1-faa0-4816-84ee-6a5e224c3d3f`。仅 GET `/demo/cpu`，同时验证 HTTP 200 和 `$.code == 0`；只读，无账号数据、无外部 AI 调用。保留用于复验。

## 双节点门禁

- 草稿 `eb8dbd31-a7c4-451f-95ec-2f9e758d7ae3`，全局 2 次/秒、30 秒，选择两台且开启“所有选中节点共同参与”，总压力不按节点翻倍。
- 本机连通性通过；专用节点 `192.168.8.131` 返回“TCP连接失败，请检查目标端口、防火墙和安全组”。
- 草稿停留等待检查，未预检、未正式启动。保留失败证据，不能计为双节点通过。
- `172.22.0.1` 在不同主机指向各自本地网络；当前实测只证明本机可达。需在专用 Agent 容器和平台隧道端分别检查，不可通过改公网放行或绕过目标保护来掩盖问题。

## AI 真实闭环

1. 旧阶梯并发 `2e6407f4-aafd-4f2f-8e05-e230b7b33b75` 重新诊断得到 v7 分析 `5d4e28c4-be72-4b2f-aaa7-0676753d1384`，证据 hash 前缀 `ff1916d09aaf`。因模型引用了不存在的证据，纠正后仍未通过，最终为 `rule_fallback`；不是 AI 建议通过。
2. 规则备用方案仍完整保留 1→2→4→1、15/15/15 秒曲线，原环境 v4、监控、P95/HTTP/业务/链路阈值与原单节点。Safari 实际完成“报告 → 下一轮完整差异 → 草稿 → 监控检查 → 连通性 → 业务预检 → 执行”，产生第一轮通过报告。
3. 人工停止轮 v7 分析已完成，证据前缀 `479547416ccd`，页面明确“AI 建议已通过校验”和“暂不升压但需要继续验证”，给出保持 2 VU、120 秒复验入口。未点击启动该建议。
4. 该 AI 仍给出线程池、连接池、下游注入等通用建议。当前 CPU 演示实现没有数据库或外部下游；模型没有足够架构约束。该状态只表示结构/证据引用/参数策略通过，不等于每条因果推断已被验证。
5. 09:32–09:33 Safari 复核最终内容：CPU 稳态 v7（f0a967e66f7b）诊断完成，标记证据不足；建议延长至 300 秒被平台调整回原 2 次/秒、180 秒，理由为尚需先排查失败，状态“AI 建议已由平台调整”。新阶梯 v7（b7b421eb5d79）因“AI 阶梯建议必须提供完整 workload 阶段曲线”而规则回退，不能算 AI 阶梯提案通过。
6. 检查发现 v7 提示词首个 JSON 示例仍缺 workload，与后文完整曲线要求不一致。本次新增不可变版本 v8：首个示例明确完整升/降压曲线，并区分四种模型字段、摘要峰值/总时长和纠错要求；不自动补齐模型缺失曲线后标记 AI 通过。活动提示词示例经实际输出校验器回归，AI/下一轮策略 PostgreSQL 88 项通过；真实模型效果仍需部署后重新诊断验证。

## 瓶颈结论与下一步实验

本轮确认“约 1.1 秒的端到端响应时间不达标”，未证明 CPU、内存、数据库或下游已经达到容量瓶颈。服务与压力机资源利用率不高只能降低部分假设的优先级，不能单独排除配额节流、GIL/排队、网络隧道或短时尖峰。

`/demo/cpu` 使用 150 ms 墙钟时间的有界哈希循环，并非固定 CPU 工作量；不能从该演示推出真实业务最大容量。

下一步按单变量补证：

1. 先核对服务器与隧道，比较演示机本地请求、平台隧道端、两台 Agent 容器相同 `/demo/ok` 和 `/demo/cpu` 的 connect/TTFB/total 耗时；记录服务处理时间，定位额外延迟位置。
2. 当前有延迟失败，先修正/解释失败来源，在原 2 次/秒、180 秒、500 ms 阈值下复验。不能仅凭 CPU 低直接升压。
3. 达标后再设计保持至少两分钟的递增阶段，记录最后稳定—首次持续失败区间；每轮保留业务断言、实际达压、队列/429、服务 CPU quota/throttling、内存和恢复证据。受控服务 4 工作槽与 0.5 核等配额须现场确认。
4. 通过目标机本地管理员入口让 `/demo/switchable` 在预检后进入 HTTP/业务故障，验证分类、保护停止和 120 秒自动恢复；不得使用一直失败的接口绕过预检门禁。
5. AI 输入补充可审计的服务架构、依赖是否存在、主动停止原因和可用指标；缺失域必须标未知/不适用，不能凭通用排查清单宣布根因。

仍未完成：演示双节点正式运行、阶梯吞吐失败复验、HTTP/业务故障切换与保护停止、真正瓶颈触达和恢复实验、数据库/线程池/GC/trace 等观测能力。此前两份阶梯历史报告不能替代本次新版闭环。

## 本轮发现并修复的代码问题

- 执行卡把阶梯起点当完整负载，并用不存在的 duration_seconds 显示 0 秒；改为完整曲线、各阶段总时长、零起点与分钟单位。
- 外层 HTTP 无条件将事件接口分派 SSE，挡住 JSON 轮询；现按 SSE ticket / Accept 分流，保留认证与资源授权。
- SSE 补 `X-Accel-Buffering: no`；无事件 10 秒转为串行、有界轮询；过期 ticket、迟到事件、重复 fallback 与页面卸载均有隔离。
- JSON 的 UUID id 不再被误作事件顺序，前端按 sequence 续取；终态超过 200 条时通过 has_more 取完末页；报告终态展示不会提前切断事件补取。
- 首次打开报告原先 mounted 和 watcher 重复建连接；现在只初始化一次，旧报告响应不得覆盖新报告。
- 独立代码审查发现的三项 P2（迟拒并行轮询、终态分页、卸载刷新）及组件联动问题均补失败回归后修复，最终审查未发现未解决 P1/P2。

另留待处理的页面问题：停止终态还残留“等待节点保存”反馈；明确 TCP 失败后汇总提示仍可能写成“等待回传”；监控补齐前的下一步文案需更直接说明等待证据。

09:03 后通过完整地址重开报告触发 Safari 启动会话错误 `The string did not match the expected pattern.`，点“重新验证会话”仍复现；此前三轮已保存且两节点释放已经确认。线上健康接口正常，错误根因尚未确认，不能把该后续阻断算为已修复。当时堡垒机尚不可用。后续用户恢复平台终端；09:30 在 Safari 强制重新载入后节点、执行、报告页面已正常，未复现该启动错误，但尚未定位其根因。

## 检查与交接

修复提交 `53d2c88`（main）。最终 Vue 88 文件 645 项通过；PostgreSQL HTTP 与 load HTTP 75 项通过；生产构建、后端静态 63 项、前端静态 84 项、Python 语法和 git diff 检查通过。没有用单元测试替代线上复验。

用户恢复平台终端后，已在 Safari 堡垒机执行既有 `bash /opt/midscene-task-platform-src/deploy/update-main-server.sh`，部署完成版本 `03bdcea`（含修复 53d2c88），8091/8088 健康、登录与前端 JS/CSS 检查通过；JS 为 index-CT0MBJv2.js。Safari 执行列表实际显示完整 1→2→4→1 VU 和 1→2→4→2 次/秒、总 45 秒，旧报告可正常打开。新执行的实时事件和停止末页补取仍待专项复验。

## 09:20 后 Safari 服务器与网络对照

- 平台堡垒机主机 10.130.4.31，隧道进程 1040744 实际连向 `root@43.157.42.206`；18080/19090 只绑定平台 Docker 桥 172.22.0.1，分别转发到演示机 127.0.0.1 同端口。没有修改密钥限制、公开端口或 FRP。
- 平台隧道入口 `/health`（不执行 CPU 工作）5 次串行 GET 全为 200。connect 秒：0.000125/0.000104/0.000082/0.000101/0.000092；TTFB 秒：0.553178/0.864681/0.863417/0.866942/0.871381；total 秒：0.869174/1.183807/1.179425/1.184895/0.871451。
- `ss -tin dst 43.157.42.206` 显示已建立 SSH 连接，TCP RTT 279.113 ms、rttvar 6.27 ms、minrtt 275.365 ms。只证明跨机链路较高延迟，不能在没有演示机本地对照时把全部健康检查耗时归为网络，也不能把 CPU 接口约 1.1 秒归为计算饱和。
- 09:30:46 Safari 两台 Agent 均心跳正常、校准有效、0 进程 / 0 VU。按用户要求后续全部浏览器操作统一 Safari，已请求在 Safari 打开演示机本机终端，不请求凭据。
